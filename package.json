{
	"license": "MIT",
	"devDependencies": {
		"lerna": "^2.11.0"
	},
  "dependencies": {},
  "scripts": {
    "start": "lerna run --parallel start",
    "postinstall": "mkdir -p packages/client/build/ && lerna run --parallel build"
  }
}
